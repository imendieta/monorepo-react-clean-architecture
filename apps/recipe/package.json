{
  "name": "recipe-app",
  "version": "1.0.0",
  "main": "./src/infrastructure/ui/Expo.tsx",
  "scripts": {
    "start": "expo start --web",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "format": "prettier --write 'src/**/*.{js,jsx,ts,tsx}'",
    "cypress": "cypress open"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.62.7",
    "@monorepo-clean-architecture/expo-config": "^0.1.0",
    "@monorepo-clean-architecture/react-native": "^0.1.0",
    "@monorepo-clean-architecture/notification": "^0.1.0",
    "@monorepo-clean-architecture/http-config": "^0.1.0",
    "tiny-invariant": "^1.3.3"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@monorepo-clean-architecture/eslint-config": "*",
    "@monorepo-clean-architecture/prettier-config": "*",
    "@monorepo-clean-architecture/typescript": "*",
    "cypress": "^13.17.0"
  }
}
